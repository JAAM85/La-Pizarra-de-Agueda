<style>

/*
 * Sidebar
 */

.sidebar {
  position: relative;
  top: 0;
  bottom: 0;
  left: 0;
  z-index: 100; /* Behind the navbar */
  padding: 0px 0 0; /* Height of navbar */
  box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1);
}

.sidebar-sticky {
  position: relative;
  top: 0;
  height:60vh;
  padding-top: .5rem;
  overflow-x: hidden;
  overflow-y: auto; /* Scrollable contents if viewport is shorter than content. */
}

@supports ((position: -webkit-sticky) or (position: sticky)) {
  .sidebar-sticky {
    position: -webkit-sticky;
    position: sticky;
  }
}

.sidebar .nav-link {
  font-weight: 500;
  color: #333;
}

.sidebar .nav-link .feather {
  margin-right: 4px;
  color: #999;
}

.sidebar .nav-link.active {
  color: #007bff;
}

.sidebar .nav-link:hover .feather,
.sidebar .nav-link.active .feather {
  color: inherit;
}

.sidebar-heading {
  font-size: .75rem;
  text-transform: uppercase;
}
</style>


 <nav class="col-md-12 d-none d-md-block bg-light sidebar">
    <div class="sidebar-sticky">
    <ul class="nav flex-column">
       <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
          <span>Administrador</span>
          <a class="d-flex align-items-center text-muted" href="#">
            <span data-feather="plus-circle"></span>
          </a>
        </h6>        
        <li class="nav-item">
        <a class="nav-link" href="<%=admintemas_path%>">
            <span data-feather="file"></span>
            Temas
        </a>
        </li>
        <li class="nav-item">
        <a class="nav-link" href="<%=adminactividades_path%>">
            <span data-feather="shopping-cart"></span>
            Actividades
        </a>
        </li>
        <li class="nav-item">
        <a class="nav-link" href="<%=admincontroles_path%>">
            <span data-feather="users"></span>
            Controles
        </a>
        </li>       
        <li class="nav-item">
        <a class="nav-link" href="<%=admintrabajos_path%>">
            <span data-feather="users"></span>
            Trabajos para casa
        </a>
        </li>              
    </ul>            
    </div>
</nav>