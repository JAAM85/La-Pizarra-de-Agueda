 <div class="container">
      <header class="blog-header py-3">
        <div class="row flex-nowrap justify-content-between align-items-center">
          <div class="col-4 pt-1">
            <% if !user_signed_in? %>
              <a class="text-muted" href="<%=new_user_session_path%>">Acceder</a>
            <%else%>
              <%= link_to('Salir', destroy_user_session_path, method: :delete, :class => "text-danger") %>
            <%end%>
          </div>
          <div class="col-4 text-center">
            <a id="lpda-desktop" class="blog-header-logo text-dark" href="/">La Pizarra De Águeda</a>
            <a id="lpda-mobile" class="blog-header-logo text-dark" href="/">LPDA</a>
          </div>
          <div class="col-4 d-flex justify-content-end align-items-center">
            <% if user_signed_in? %>
              <a class="text-muted"><%=current_user.name%></a>            
            <%end%>
          </div>
        </div>
      </header>

      <div class="nav-scroller py-1 mb-2">
        <nav class="nav d-flex justify-content-between">
          <a class="p-2 text-muted" href="/">Inicio</a>
          <a class="p-2 text-muted" href="#">Actividades</a>
          <a class="p-2 text-muted" href="/cursos/1">Primero</a>
          <a class="p-2 text-muted" href="/cursos/2">Segundo</a>
          <a class="p-2 text-muted" href="#/cursos/3">Tercero</a>
          <a class="p-2 text-muted" href="/cursos/4">Cuarto</a>
          <a class="p-2 text-muted" href="/cursos/5">Quinto</a>
          <a class="p-2 text-muted" href="/cursos/6">Sexto</a>
          <a class="p-2 text-muted" href="#">Sobre la profesora</a>
          <a class="p-2 text-muted" href="#">Enlaces de interés</a>
        </nav>
      </div>
  </div>
